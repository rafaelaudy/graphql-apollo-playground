type Link {
  id: ID! @unique
  createdAt: DateTime!
  description: String!
  url: String!
  postedBy: User
}

type User {
  id: ID! @unique
  createdAt: DateTime!
  email: String! @unique
  password: String!
  links: [Link!]!
}
